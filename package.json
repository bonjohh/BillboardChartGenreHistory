{
  "name": "billboard-chart-genre-history",
  "version": "0.0.0",
  "description": "BillboardChartGenreHistory",
  "main": "main.ts",
  "author": {
    "name": "bonjohh"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "tsx watch src/index.ts",
    "watch": "tsc -w -p ."
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "tsx": "^4.17.0",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "date-fns": "^3.6.0"
  }
}
